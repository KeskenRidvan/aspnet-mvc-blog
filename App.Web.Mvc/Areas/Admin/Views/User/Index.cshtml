@model IEnumerable<App.Persistence.Data.Entity.User>
@{
  ViewData["Title"] = "User Admin Page";
}
<div class="container">
  <div class="row mb-2">
    <div class="col">
      <a class="btn btn-primary" asp-controller="User" asp-action="Create"><i class="bi-plus-circle"></i> Yeni Kullanıcı Ekle</a>
    </div>
  </div>
  <div class="row mb-2">
    <div class="col">
      <div class="card">
        <div class="card-header">
          <h5>Kullanıcı Listesi</h5>
        </div>
        <div class="card-body">
          <table class="table table-striped">
            <thead>
              <tr>
                <th scope="col">#</th>
                <th scope="col">Değiştir/Sil</th>
                <th scope="col"><label asp-for="@Model.FirstOrDefault().Name ?? " Kullanıcı Adı""></label></th>
                <th scope="col"><label asp-for="@Model.FirstOrDefault().Surname ?? " Kullanıcı Soyadı""></label></th>
                <th scope="col"><label asp-for="@Model.FirstOrDefault().Email ?? " Kullanıcı E-mail""></label></th>
              </tr>
            </thead>
            <tbody>
              @foreach (var (item, i) in Model.Select((item, i) => (item, i)))
              {
                <tr>
                  <th scope="row">@(i + 1)</th>
                  <th>
                    <a class="m-2" asp-controller="User" asp-action="Edit" asp-route-id="@item.Id"><i class="bi-pencil-square text-success"></i></a>
                    <a class="m-2" asp-controller="User" asp-action="Delete" asp-route-id="@item.Id"><i class="bi-trash text-danger"></i></a>
                  </th>
                  <td>@item.Name</td>
                  <td>@item.Surname</td>
                  <td>@item.Email</td>
                </tr>
              }
            </tbody>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>
